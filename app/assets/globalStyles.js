import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    container: {
        marginTop: 50,
        paddingLeft: 30,
        paddingRight: 30,
        marginBottom: 30,
    }
})
